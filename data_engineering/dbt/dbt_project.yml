name: 'heliostream'
version: '0.1.0'
config-version: 2
profile: 'heliostream'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  heliostream:
    staging:
      +materialized: view
    marts:
      +materialized: table
